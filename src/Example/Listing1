struct Params {
	address: array[int],
	payload: any,
	sender: string
}
service BasicService : (any,any) -> void;
fn function(isPremiumUser: bool, par: any) -> void {
	if ( Params.sender == "ciao"  ) {
		call PremiumService();
	} else {
		call BasicService( par );
	}
}