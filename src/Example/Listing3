service Map :(any, int) -> void;
service Reduce :(any, int) -> void;
fn main(jobs:any, m:int , r:int) ->void {
      for(i in (0,m)){
          call Map(jobs,i);
          for (j in (0,r)){
            call Reduce(jobs, i ,j);
          }
      }
 }