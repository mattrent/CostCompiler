struct Params {
	address: array[int],
	payload: any,
	sender: string
}
service PremiumService : (string) -> void;
service BasicService : (any, null) -> void;
fn function(isPremiumUser: bool, par: any) -> void {
	if ( isPremiumUser ) {
		call PremiumService(1838492);
	} else {
		call BasicService( par , par);
	}
}