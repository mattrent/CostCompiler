struct Params {
	counter: int
}
struct Result {
	payload: int
}

fn loop(c: int, acc: int) -> int {
	if (c > 0) {
		return loop(c - 1, acc + c)
	} else {
		return acc
	}
}

(par: Params) => {
	let
		counter = par.counter * 2
		result = loop(counter, 0)
	in
		return Result {
			payload: result
		}
}