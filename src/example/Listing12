payload : any[32]
service BasicService: (int) -> any;
fn svc(i: int) -> any{
    if(i == 0){
        return 0
    }else{
        for(j in (0,32)){
            return call BasicService(j)
        }
    }
}
(len : int) => {
	return svc(len)
}